// // Find all the possible paris that adds up to the target sum

// // input , [5,1,1,5,3,3] target = 6 
// // output , [[5,1], [5,1], [1,5], [3,3]]


// let nums = [5,1,1,5,3,3]
// let target = 6
// let pairsSum = (arr, target) => {
//     // Use nested for loop, outer loop is each number, inner loops is comparing each number with the rest. (like bubble sort)
    
//     for (let i=0; i.arr.length; i++){
//         // Inner for loop will excute the whole loops for each outer loop iteration. **
//         // When i is 0, arr[0] = 5
//         // Inner loop will compare 5 with the rest of the numbers.
//         // Inner loop will increment j
//         // j starts in position i, because when i is 2, you dont want to compare i with the ones that 's already been compared, which are 5,1.
//             for(let j=i; j<arr.length; j++){
//                 // See what i and j are within each iteration.
                
//             }
//     }
// }

// pairsSum(nums, target)

//bubble sort

let numsArr = [5,6,10,3,2,1,15]

let bubbleSort = (arr) => {
        // Outer loop will loop 7 times, 
        for (let i=0; i<arr.length; i++) 
            // First outer loop, inner will compare 6 times
                for(let j=0; j<arr.length-1;j++){
                    
                }
}